# MCP 运行日志(不在仓库维护范围)
logs/

# 本地计划/工作文档
.hermes/

# 照片原图暂存(压缩后才会入库;照片实际存 R2,不入仓库)
photos/

# 本机 Agent 记忆/工作文档(含个人行程信息,勿进公开仓库)
.workbuddy/

# Node / wrangler
node_modules/
.wrangler/

# 本地密钥/环境变量(含 SESSION_SECRET 等;生产环境同值以 Cloudflare Secrets 存放)
.dev.vars

# macOS
.DS_Store
