{
  "name": "gugugaga",
  "version": "2.0.0",
  "private": true,
  "description": "咕咕嘎嘎 - 通用旅行日记 portal(Cloudflare Pages + Functions + R2 + KV)",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name gugugaga"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "tiny-pinyin": "^1.3.2"
  }
}
